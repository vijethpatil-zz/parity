(function() {var implementors = {};
implementors['ethsync'] = ["impl NetworkProtocolHandler&lt;SyncMessage&gt; for <a class='struct' href='ethsync/struct.EthSync.html' title='ethsync::EthSync'>EthSync</a>",];

            if (window.register_implementors) {
                window.register_implementors(implementors);
            } else {
                window.pending_implementors = implementors;
            }
        
})()
